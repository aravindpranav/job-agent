"""job_agent — discover fresh ATS jobs and score their fit with an LLM.

Slice 1 scope: discovery (Greenhouse, Lever, Ashby, SmartRecruiters), a 24h
freshness filter, dedup, keyword pre-filtering, LLM fit scoring, and a CLI that
prints a ranked table. Resume tailoring, an answer bank, and application assist
are later slices (see the stub modules).
"""

__version__ = "0.1.0"
